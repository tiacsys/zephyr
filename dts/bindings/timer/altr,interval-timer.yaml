description: Altera Avalon Interval Timer

compatible: "altr,interval-timer"

include: base.yaml

properties:
    reg:
      required: true

    interrupts:
      required: true

    label:
      required: true

    clock-frequency:
      type: int
      required: true
      description: Clock frequency information for Timer operation
